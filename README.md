# Docker Mission — Starter

Ce projet accompagne l'exercice fil rouge Docker.

Les fichiers `Dockerfile` et `compose.yaml` sont volontairement incomplets.

## Arborescence

```text
docker-mission/
├── web/
│   ├── index.html
│   └── Dockerfile
├── logs/
├── compose.yaml
├── REPONSES.md
└── README.md
```

Suivez l'énoncé du formateur pour compléter le projet.
